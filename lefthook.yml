pre-commit:
  parallel: false
  commands:
    format:
      run: |
        deno task fmt
        git add -u
      glob: "*.{ts,tsx,js,jsx,json,md}"
    lint:
      run: deno task lint
      glob: "*.{ts,tsx,js,jsx}"
