pre-commit:
  parallel: false
  commands:
    format:
      run: deno task fmt
      glob: "*.{ts,tsx,js,jsx,json,md}"
      stage_fixed_files: true
    lint:
      run: deno task lint
      glob: "*.{ts,tsx,js,jsx}"
