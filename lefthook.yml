pre-commit:
  parallel: false
  commands:
    format:
      run: |
        deno task fmt | grep "^/" | xargs -r git add
      glob: "*.{ts,tsx,js,jsx,json,md}"
    lint:
      run: deno task lint
      glob: "*.{ts,tsx,js,jsx}"
